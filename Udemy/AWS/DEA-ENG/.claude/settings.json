{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.md) ./node_modules/.bin/markdownlint-cli2 --fix \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Formateando Markdown con markdownlint-cli2..."
          }
        ]
      }
    ]
  }
}
